Fix YENE agent dashboard forms still not showing clearly.

Current issue:
- Codex says labels/placeholders were added, but browser still shows mostly blank fields.
- Need force-visible form UI, not hidden by CSS.
- Also ensure server is rendering latest template, not old cached copy.

Files:
- templates/agent_dashboard.html
- app.py only if needed

Fix:
1. Add strong CSS scoped only to agent registration forms:
   #driverForm label, #clientForm label visible
   #driverForm input/select, #clientForm input/select visible
   background white or dark readable
   color readable
   border visible
   min-height 48px
   width 100%
   placeholder color readable
   display block
   opacity 1
   visibility visible

2. Add visible helper text under each field:
   Driver phone number
   Driver PAR code
   Client phone number
   Client CUS code

3. Confirm actual rendered HTML contains:
   Driver phone number
   Driver PAR code
   Client phone number
   Client CUS code

4. Add template version marker visible only small at bottom:
   Dashboard version: registration-ui-v3
   Use normal user-friendly text, not developer wording.

5. Disable template cache in local/dev if needed:
   app.config["TEMPLATES_AUTO_RELOAD"] = True

6. Run:
python3 -m py_compile app.py agent_dashboard_v4.py

7. Extract JS and run node --check.

Final output:
- exact CSS/HTML fix
- whether version marker added
- tests run
